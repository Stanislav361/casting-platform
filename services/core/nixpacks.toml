[phases.setup]
nixPkgs = ["python312", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m alembic upgrade heads && python -m uvicorn main.main_prod:app --host 0.0.0.0 --port ${PORT:-8000}"
