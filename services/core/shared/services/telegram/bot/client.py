"""Единый экземпляр бота на весь процесс.

НЕ ОБОРАЧИВАЙТЕ `bot` в `async with`. Aiogram в `Bot.__aexit__` закрывает
HTTP-сессию вместе с TCP-коннектором, а объект `bot` здесь общий для всего
приложения. Поэтому `async with bot` обрывает не только свой запрос, но и все
запросы, которые в этот момент выполняются параллельно: короткое уведомление
успевает уйти и закрывает сессию, а долгая загрузка обложки кастинга в канал
падает с `TelegramNetworkError: ClientConnectionError: Connector is closed`.
Именно из-за этого посты уходили в канал без картинки.

Вызывайте методы напрямую — `await bot.send_photo(...)`. Сессия создаётся
лениво при первом запросе и переиспользуется до конца жизни процесса.
"""
from aiogram import Bot
from config import settings

bot = Bot(token=settings.TG_BOT_TOKEN)
